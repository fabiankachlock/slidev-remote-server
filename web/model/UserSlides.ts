export type UserSlidesPreview = {
  id: string;
  coverUrl: string;
  title: string;
  uploaded: number;
};

export type UserSlideData = {
  id: string;
  owner: string;
  title: string;
  uploadDate: number;
  coverUrl: string;
  folderUrl: string;
};
